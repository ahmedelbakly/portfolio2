# Canonical origin, substituted into index.html as %VITE_SITE_URL%.
# Must match the `base` in vite.config.ts. No trailing slash.
VITE_SITE_URL=https://ahmedelbakly.github.io/portfolio

# EmailJS — these are browser-side identifiers by design and are safe to ship.
# Rotate them from the EmailJS dashboard, not by hiding them here.
VITE_EMAILJS_SERVICE_ID=service_ael1zmy
VITE_EMAILJS_TEMPLATE_ID=template_7qz5pz8
VITE_EMAILJS_PUBLIC_KEY=SxCsnUXdXPis14lcN
